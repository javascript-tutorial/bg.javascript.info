Отговорът: `3`.

```js run
alert( null || 2 && 3 || 4 );
```

Приоритетът на "И" `&&` е по-висок от "ИЛИ" `||`, така че то се изпълнява първи.

Резултатът от `2 && 3 = 3`, така изразът става:

```
null || 3 || 4
```

Сега резултатът е първата вярна стойност: `3`.

