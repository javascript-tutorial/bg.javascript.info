В кода по-долу всеки ред съответства на елемента в списъка със задачи.

```js run
let admin, name; // може да декларираме две променливи наведнъж

name = "John";

admin = name;

alert( admin ); // "John"
```
